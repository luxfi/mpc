run:
  timeout: 5m

issues:
  exclude-dirs:
    - vendor
    - .git
  exclude-files:
    - ".*_test.go"

linters-settings:
  goimports:
    local-prefixes: github.com/luxfi/mpc

linters:
  enable:
    - goimports
    - govet
    - ineffassign
    - misspell
    - unconvert
    - typecheck
  disable:
    - errcheck
    - gosimple
    - staticcheck
    - unused

issues:
  exclude-use-default: false
  max-issues-per-linter: 0
  max-same-issues: 0