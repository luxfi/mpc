steps:
  - name: 'gcr.io/kaniko-project/executor:latest'
    args:
      - '--destination=us-docker.pkg.dev/$PROJECT_ID/backend/mpc:next'
      - '--build-arg=GITHUB_TOKEN=${_GITHUB_TOKEN}'
      - '--cache=true'
      - '--cache-ttl=72h'
options:
  machineType: 'E2_HIGHCPU_8'
timeout: '600s'
