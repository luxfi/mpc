apiVersion: v1
kind: Service
metadata:
  name: mpc-dashboard-svc
  namespace: lux-mpc
  labels:
    app: mpc-dashboard
    app.kubernetes.io/part-of: lux-mpc
spec:
  type: ClusterIP
  ports:
    - port: 3000
      targetPort: 3000
      protocol: TCP
      name: http
  selector:
    app: mpc-dashboard
